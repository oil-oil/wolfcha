---
trigger: manual
---

- 强制解耦 (SRP)：UI 组件仅负责渲染。所有状态管理、数据获取和复杂计算必须抽离为 Custom Hooks 或 Utils 函数。
- 拒绝魔法与冗余 (DRY)：禁止出现魔法数字或硬编码字符串，统一定义为常量 (Constants/Enums)。发现重复逻辑立即封装复用。
- 代码即文档：变量与函数命名必须精准语义化，代替陈旧或显而易见的注释。仅在复杂业务逻辑处保留解释性注释。
- 小文件原则：单个文件若逻辑过于复杂或过长，必须主动拆分为子组件或独立模块。